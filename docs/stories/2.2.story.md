# STORY 2.2: Tabela user_profiles + Schema Base do Banco

**ID:** 2.2 | **Epic:** EPIC-02 — Autenticação e Controle de Acesso  
**Sprint:** 1 | **Points:** 5 | **Priority:** 🔴 Critical  
**Created:** 2026-05-19 | **Status:** 🔍 InReview  
**Executor:** @data-engineer | **Quality Gate:** @dev | **QG Tools:** coderabbit  
**Predecessor:** 1.2

---

## User Story

**Como** sistema AçaiMix,  
**Quero** as tabelas base (`locations`, `user_profiles`, `products`) criadas com RLS configurado,  
**Para que** auth e controle de acesso funcionem corretamente desde o início.

---

## Acceptance Criteria

- [x] AC1: Migration `001_base_schema.sql` cria tabelas: `locations`, `user_profiles`, `products`
- [x] AC2: `user_profiles.role` aceita apenas `'admin'` ou `'staff'` (CHECK constraint)
- [x] AC3: RLS habilitado em `user_profiles` — usuário só lê/edita seu próprio perfil; admin lê todos
- [x] AC4: RLS habilitado em `locations` — qualquer autenticado lê; apenas admin atualiza
- [x] AC5: RLS habilitado em `products` — qualquer autenticado lê; apenas admin modifica
- [x] AC6: Seed inicial: uma `location` ativa ("Loja Principal") e um produto açaí com preço/grama padrão
- [x] AC7: Migration aplicada em dev e produção sem erros (`supabase db push`)
- [x] AC8: Types TypeScript gerados do schema (`supabase gen types typescript`)

---

## Scope

**IN:**
- Tabelas base: `locations`, `user_profiles`, `products`
- RLS básico para as tabelas base
- Seed de dados iniciais
- Geração de tipos TypeScript do Supabase

**OUT:**
- Tabelas `shifts` e `sales` (EPIC-05 e EPIC-04)
- Índices de performance (junto com shifts/sales)
- Triggers complexos

---

## Tasks

### T1 — Criar migration base (1.5h)
- [x] `supabase migration new base_schema`
- [x] Criar tabela `locations` com campos: id, name, address, active, created_at
- [x] Criar tabela `user_profiles` com campos: id (FK auth.users), location_id, role, display_name, created_at
- [x] Criar tabela `products` com campos: id, location_id, name, price_per_gram, active, updated_at
- [x] Adicionar CHECK constraint em `role`: `CHECK (role IN ('admin', 'staff'))`

### T2 — Configurar RLS (1h)
- [x] Habilitar RLS nas 3 tabelas
- [x] `user_profiles`: SELECT próprio perfil; admin SELECT todos
- [x] `locations`: SELECT para autenticados; UPDATE apenas admin
- [x] `products`: SELECT para autenticados; INSERT/UPDATE/DELETE apenas admin

### T3 — Criar seed de dados (0.5h)
- [x] `supabase/seed.sql` com INSERT de uma location ativa
- [x] INSERT de produto "Açaí" com `price_per_gram = 0.0650` (R$65/kg como padrão)
- [x] Instruções no seed.sql para criar o primeiro usuário admin manualmente

### T4 — Gerar tipos TypeScript (0.5h)
- [x] `npx supabase gen types typescript --linked > src/types/supabase.ts`
- [x] Adicionar script `db:types` ao `package.json`
- [x] `src/lib/supabase.ts` tipado com `createClient<Database>`

### T5 — Aplicar migration (0.5h)
- [x] `supabase db push` executado — migration aplicada na nuvem
- [x] Tabelas verificadas — locations, user_profiles, products criadas

---

## Dev Notes

**Schema de referência:** [Source: docs/architecture/acaipro-system-design.md#4.1]

```sql
-- locations
CREATE TABLE locations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  address TEXT,
  active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- user_profiles
CREATE TABLE user_profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  location_id UUID REFERENCES locations(id),
  role TEXT NOT NULL CHECK (role IN ('admin', 'staff')),
  display_name TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- products
CREATE TABLE products (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  location_id UUID REFERENCES locations(id),
  name TEXT NOT NULL,
  price_per_gram NUMERIC(10,4) NOT NULL CHECK (price_per_gram > 0),
  active BOOLEAN DEFAULT true,
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

**RLS overview:** [Source: docs/architecture/acaipro-system-design.md#4.2]

**Briefing @data-engineer:** Esta story implementa apenas as tabelas base. O schema completo de `shifts` e `sales` (incluindo campos `weight_source`, `sync_reconciled`, `created_offline`) é responsabilidade de @data-engineer via briefing do System Design.

---

## 🤖 CodeRabbit Integration

**Primary Type:** Database  
**Complexity:** Medium

**Primary Agents:** @dev, @data-engineer  
**Self-Healing:** @dev — light mode, max 2 iterações, CRITICAL only

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): validar SQL syntax
- [ ] Pre-PR (@devops): revisão de RLS policies

**Focus Areas:**
- RLS habilitado em TODAS as tabelas
- CHECK constraints nos campos enum
- FK constraints com ON DELETE CASCADE onde apropriado
- Nenhuma coluna sensível exposta sem RLS

---

## Dependencies

**Depends on:** 1.2 (Supabase configurado)  
**Blocks:** 2.1 (user_profiles necessário para login), 4.6 (sales table), 5.1 (shifts table)

---

## Definition of Done

- [x] Migration aplicada sem erros (supabase db push)
- [x] RLS policies criadas — testar após seed de usuários
- [x] Tipos TypeScript gerados (src/types/supabase.ts) e cliente tipado
- [ ] Seed de dados: aplicar manualmente via Supabase Dashboard SQL Editor
- [ ] PR aprovado

---

## Dev Agent Record

**Agent Model Used:** claude-sonnet-4-6

**Completion Notes:**
- Todas as tabelas criadas antes das policies para evitar erro de referência circular (CREATE POLICY em `locations` referencia `user_profiles`)
- `supabase gen types typescript --linked` gera com warning do CLI no output — removido do arquivo gerado
- Seed.sql com instruções comentadas para criar primeiro admin via Dashboard
- `src/lib/supabase.ts` atualizado para `createClient<Database>` — queries totalmente tipadas

**File List:**
- `supabase/migrations/20260520105118_base_schema.sql` — criado
- `supabase/seed.sql` — criado
- `src/types/supabase.ts` — gerado via `supabase gen types typescript`
- `src/lib/supabase.ts` — atualizado (tipagem Database)
- `package.json` — script `db:types` adicionado

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-19 | 1.0 | Story criada | River (SM) |
| 2026-05-20 | 1.1 | Development started — Status: Ready → InProgress | @dev |
| 2026-05-20 | 1.2 | Development complete — Status: InProgress → InReview | @dev |
