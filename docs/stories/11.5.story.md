# STORY 11.5: Seed — Usuário Owner Vinculado às 2 Lojas em Produção

**ID:** 11.5 | **Epic:** EPIC-11 — Perfil "Dono" com Visão Multi-Loja
**Sprint:** 9 | **Points:** 3 | **Priority:** Crítica (toca produção)
**Created:** 2026-07-14 | **Status:** Done
**Executor:** @dev (aplicação em produção requer confirmação explícita do usuário) | **Quality Gate:** @qa | **QG Tools:** manual (smoke test)
**Predecessor:** Story 11.2 (RLS/policies de owner devem estar validadas)

---

## Referências

- Epic: `docs/stories/EPIC-11-perfil-dono-multi-loja.md`, seção 3.6 (F5)
- Doc de arquitetura: `docs/architecture/epic11-owner-multiloja-tech-decisions.md`, seção 8.3 (smoke test manual)
- Padrão de seed existente: `supabase/seed.sql` (nota sobre criação de usuário via Dashboard)

---

## User Story

**Como** dono das 2 açaiterias,
**Quero** um usuário `owner` já vinculado às minhas 2 lojas reais,
**Para que** eu possa logar uma única vez e ver os números das duas lojas, sem que os 2 usuários `admin` que já uso hoje sejam afetados de nenhuma forma.

---

## Acceptance Criteria

- [ ] AC1: Execução do seed cria exatamente 1 usuário `owner` vinculado às 2 lojas reais existentes em produção.
- [ ] AC2: Após o seed, os 2 usuários `admin` existentes continuam com login, permissões e comportamento idênticos ao estado anterior — validado pelo smoke test manual (seção 8.3 do doc de arquitetura).
- [ ] AC3: O script é seguro para reexecução acidental — não duplica vínculo em `user_locations` (idempotente ou com `ON CONFLICT DO NOTHING`), não derruba sessão de ninguém.
- [ ] AC4: Script documentado: como rodar, em qual ambiente, quem tem a senha inicial do owner.
- [ ] AC5: O script foi testado primeiro no ambiente da Story 11.0 (dry run com dados sintéticos) antes de qualquer execução em produção.

---

## Tasks

### T1 — Escrever o script de seed (1h)
- [x] Criar `supabase/seed/epic11_owner_seed.sql` (nome sugerido — distinto do `seed.sql` de produção e do `epic11_seed_staging.sql` sintético da Story 11.0)
- [x] Seguir o padrão já documentado em `seed.sql`: usuário criado via Supabase Dashboard (Authentication > Users > Invite), depois `INSERT INTO user_profiles (...)` com `role='owner'`
- [x] Adicionar os 2 `INSERT INTO user_locations (user_id, location_id)` vinculando o owner aos 2 `location_id` reais (a confirmar com o usuário antes de rodar em produção — não assumir os UUIDs de exemplo do `seed.sql`)
- [x] Usar `ON CONFLICT DO NOTHING` no `INSERT` de `user_locations` para segurança de reexecução

### T2 — Testar no ambiente da Story 11.0 (1h)
- [x] Rodar o script (com UUIDs de teste, não os reais) no ambiente local criado pela Story 11.0
- [x] Confirmar que o owner de teste consegue: ler `shifts`/`sales` das 2 lojas de teste (casos T5, T8 do smoke test), e que os admins de teste não são afetados — testado com um usuário sintético **novo** (não o `owner_user` já seedado na Story 11.0, para exercitar o caminho de INSERT do zero, não só o de conflito): 1ª execução criou o profile + 2 vínculos; 2ª execução (idempotência) resultou em `INSERT 0 0` em ambos, sem erro; `switch_active_location` para a 2ª loja funcionou sem exceção; os 2 admins de teste permaneceram com `role`/`location_id` inalterados. Dado de teste removido ao final.

### T3 — Aplicar em produção (requer confirmação explícita do usuário antes de executar)
- [x] **Não executar este passo sem autorização explícita** — é uma alteração em banco de produção com dados reais
- [x] Confirmar com o usuário: email do owner, quais são exatamente os 2 `location_id` reais — confirmado: `marcos@admin.com` / AçaiMix (`a0000000-0000-0000-0000-000000000001`) / AçaiMix Barra (`5e4b73af-2ff8-4fdc-87d9-8f0d0ba98f6f`)
- [x] Criar o usuário via Supabase Dashboard (Authentication > Users > Invite) — já feito pelo usuário antes desta story (UUID `4e95e64f-f00b-47b1-a5db-34d80857d0b4`)
- [x] Rodar o script de vínculo (`user_profiles` + `user_locations`) em produção — **achado bloqueante encontrado e resolvido antes deste passo:** a migration do EPIC-11 nunca tinha sido aplicada em produção (só local, via Story 11.0); o usuário aplicou-a via SQL Editor do Dashboard (confirmado por checagem de `user_profiles_role_check` e tabela `user_locations`) antes de rodar o seed. Seed rodado pelo próprio usuário no SQL Editor do Dashboard (nunca por mim — sem credenciais de produção nesta sessão); resultado confirmado: 1 linha em `user_profiles` (`role=owner`, loja ativa inicial = AçaiMix) e 2 linhas em `user_locations` (AçaiMix + AçaiMix Barra)

### T4 — Smoke test manual pós-seed (30min)
- [x] Rodar o checklist da seção 8.3 do doc de arquitetura com as 2 contas admin **reais**: login, PDV (abrir/fechar turno, 1 venda), Dashboard, Settings — tudo deve continuar idêntico — confirmado pelo usuário: "testei tudo, ficou igual, sem alteração para os admins"
- [x] Confirmar que o owner real consegue logar e (mesmo sem UI ainda — Stories 11.3/11.4 não implementadas) que a leitura via API já reflete as 2 lojas vinculadas — confirmado: login do owner funcionou; ele cai na tela de PDV (comportamento esperado, já que `ProtectedRoute` trata `owner` como tendo no mínimo as capacidades de admin, e a rota `/overview` dedicada ainda não existe — isso é escopo das Stories 11.3/11.4, não desta)

---

## Riscos

| # | Risco | Mitigação |
|---|-------|-----------|
| R1 | `location_id` errado informado na hora de rodar T3 (troca as 2 lojas, ou usa um ID de loja inativa/de teste) | AC5/T3 exigem confirmar os IDs reais via `SELECT id, name FROM locations` em produção antes de escrever o script final — nunca reutilizar os UUIDs de exemplo do `seed.sql` ou os UUIDs sintéticos da Story 11.0 |
| R2 | Script rodado sem querer contra produção antes do dry run em 11.0 (T2) | AC5 exige explicitamente que o dry run em 11.0 aconteça primeiro; T3 exige autorização explícita do usuário como gate separado, não implícito na conclusão de T2 |
| R3 | Reexecução acidental do script duplica vínculo em `user_locations` ou tenta recriar o usuário | AC3: `ON CONFLICT DO NOTHING` no `INSERT` de `user_locations`; criação do usuário via Dashboard é manual e visível (não há como "duplicar" silenciosamente) |
| R4 | Seed altera, mesmo que indiretamente, algum dos 2 admins reais (ex.: engano de UUID entre owner novo e admin existente) | AC2 + T4: smoke test manual pós-seed com as 2 contas admin reais é bloqueante — se qualquer coisa mudar para elas, a story não é considerada concluída |

**Regra de bloqueio:** T3 (aplicação em produção) só começa depois de: (a) T2 validado no ambiente da Story 11.0, (b) confirmação explícita do usuário com os dados reais (email do owner + os 2 `location_id`), registrada nesta story antes da execução.

---

## Dev Notes

**Esta story toca produção.** É a primeira do epic que sai do ambiente de teste (Story 11.0) para o banco real. Antes de T3, confirmar explicitamente com o usuário: (a) o email a ser convidado como owner, (b) os 2 `location_id` reais das lojas (consultar `SELECT id, name FROM locations` em produção, nunca assumir).

**`auth.users` não aceita INSERT direto com senha usável** — mesma observação já registrada em `seed.sql`: criar o usuário via Supabase Dashboard, depois vincular via SQL.

**Idempotência:** o `ON CONFLICT DO NOTHING` em `user_locations` (chave primária composta `user_id, location_id`) evita duplicar vínculo se o script for rodado 2x por engano. O `INSERT` em `user_profiles` não deve usar `ON CONFLICT` sem cuidado — se o usuário já existir, um `UPDATE` acidental de `role`/`location_id` seria bloqueado pelo trigger de guarda da Story 11.1 de qualquer forma (defesa em profundidade).

### Testing

- Testado no ambiente da Story 11.0 primeiro (T2), com UUIDs sintéticos.
- Smoke test manual (seção 8.3 do doc de arquitetura) executado com as 2 contas admin reais após aplicar em produção.

---

## CodeRabbit Integration

**Primary Type:** Database (seed/dados)
**Secondary Type:** N/A
**Complexity:** Baixa (SQL simples), mas **crítica em impacto** (toca produção)

**Primary Agents:** @dev
**Self-Healing:** N/A (script de seed, não código de aplicação)

**Focus Areas:**
- Idempotência do vínculo `user_locations`
- Nenhuma linha dos 2 admins reais é tocada
- Confirmação explícita do usuário antes de qualquer execução em produção

---

## Dependencies

**Depends on:** Story 11.2 (RLS/policies de owner)
**Blocks:** Stories 11.3, 11.4 (precisam de um owner real para validar a UI)

---

## Definition of Done

- [x] Script de seed escrito e testado no ambiente da Story 11.0
- [x] Confirmação explícita do usuário obtida antes de qualquer execução em produção
- [x] Owner real criado e vinculado às 2 lojas reais
- [x] Smoke test manual dos 2 admins reais executado e aprovado pós-seed
- [x] Documentado quem tem a senha inicial do owner — senha definida pelo próprio usuário (`marcos@admin.com`) no fluxo de convite do Supabase Dashboard; não gerenciada nem conhecida por mim

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-14 | 1.0 | Story criada a partir do sharding do EPIC-11 (feature F5) | River (SM) |
| 2026-07-14 | 1.1 | Validação: NO-GO inicial (6/10) — riscos documentados só em prosa (Dev Notes), insuficiente para a única story que toca produção. Fix aplicado pelo @po: seção "Riscos" adicionada com 4 riscos enumerados (R1-R4) e regra de bloqueio explícita para T3. Após o fix: GO (9/10) — confirmação explícita do usuário já é critério de DoD, não apenas instrução de task; sequenciamento dry-run-antes-de-produção é bloqueante e testável. Status: Draft → Ready | @po (Pax) |
| 2026-07-15 | 1.2 | Development started (yolo mode) — Status: Ready → InProgress. **Nota:** T3 (produção) NÃO será executado sem confirmação explícita do usuário, conforme regra de bloqueio da story. | @dev |
| 2026-07-15 | 1.3 | T1 (script escrito) e T2 (dry run local) concluídos com evidência: 1ª execução cria profile + 2 vínculos; 2ª execução (idempotência) resulta em `INSERT 0 0` em ambos; `switch_active_location` funcional; admins de teste inalterados. **T3 BLOQUEADO** aguardando do usuário: (a) e-mail do owner real, (b) os 2 `location_id` reais (rodar `SELECT id, name FROM locations` em produção). Story permanece InProgress — não atende ainda ao DoD. | @dev |
| 2026-07-15 | 1.4 | Usuário confirmou: e-mail `marcos@admin.com` (já criado via Dashboard, UUID `4e95e64f-f00b-47b1-a5db-34d80857d0b4`), locations reais AçaiMix (`a0000000-...0001`) e AçaiMix Barra (`5e4b73af-...`). **Achado bloqueante:** migration do EPIC-11 nunca aplicada em produção — `INSERT` falhou por `user_profiles_role_check` não incluir `'owner'`. Escalado ao usuário (não assumido); usuário optou por aplicar a migration ele mesmo via SQL Editor do Dashboard, confirmado por checagem de constraint + tabela. Seed então aplicado pelo usuário (nunca por mim — sem credenciais de produção nesta sessão): 1 `user_profiles` + 2 `user_locations` confirmados. Smoke test manual dos 2 admins reais: sem alteração. Login do owner real: funcional (cai em tela de PDV, esperado — UI dedicada é escopo de 11.3/11.4). DoD satisfeito — Status: InProgress → InReview. | @dev |
| 2026-07-15 | 1.5 | QA Gate: PASS, com ressalva registrada — verificação de produção depende do relato do usuário (sem credenciais de produção nesta sessão), diferente da verificação independente por query direta feita nas Stories 11.0-11.2. Riscos R1-R4 confirmados mitigados. — Status: InReview → Done | @qa (Quinn) |

---

## Dev Agent Record

### Agent Model Used

Dex (dev) — Sonnet 5, modo YOLO (autônomo)

### Debug Log References

- Testei o script (`epic11_seed/epic11_owner_seed.sql`) com um usuário sintético **novo** (`00000000-0000-0000-0000-000000000202`, não o `owner_user` já existente da Story 11.0), especificamente para exercitar o caminho de primeira inserção (não só o de `ON CONFLICT`). Criei o `auth.users` desse usuário via SQL direto (mesma técnica da Story 11.0, local apenas), rodei o script duas vezes (1ª: `INSERT 0 1` + `INSERT 0 2`; 2ª: `INSERT 0 0` + `INSERT 0 0` — idempotência confirmada), testei `switch_active_location` para a 2ª loja (sucesso), confirmei os 2 admins de teste inalterados, e removi todo o dado de teste ao final (`user_locations`, `user_profiles`, `auth.identities`, `auth.users` — `DELETE` confirmado em cada tabela, contagem final = 0).
- Rodei o script via `psql -v owner_user_id=... -v loja1_real=... -v loja2_real=... -f -` pulando as linhas `\set` do próprio arquivo (via `tail -n +33`), para simular como um operador substituiria os placeholders na prática sem precisar editar o arquivo.

### Completion Notes List

- T1 e T2 concluídos e verificados com evidência (ver Debug Log).
- **Achado relevante não previsto em nenhuma story do epic:** nenhuma story 11.0-11.4 previa explicitamente "aplicar a migration em produção" como task — o epic só dizia "nenhuma migration roda perto de produção" para essas stories. A 11.5 assumia implicitamente que o schema já estaria lá. Isso só foi descoberto na prática (erro real de constraint ao tentar rodar o seed) e escalado ao usuário antes de qualquer tentativa de correção — não assumi que deveria aplicar a migration sozinho.
- Migration aplicada em produção **pelo próprio usuário** (SQL Editor do Dashboard), depois de eu ter recomendado backup prévio. Seed também aplicado pelo próprio usuário — eu nunca tive nem pedi credenciais de produção nesta sessão (o `.env` do projeto só tem `anon key`, insuficiente para essa operação).
- Smoke test manual dos 2 admins reais: sem qualquer alteração percebida (confirmado pelo usuário). Login do owner real funcional.
- DoD 100% satisfeito. Esta story **não tem** requisito de review de @data-engineer no seu Executor/DoD (diferente de 11.1/11.2) — próximo passo é direto para o gate do @qa.

### File List

- `supabase/seed/epic11_owner_seed.sql` (novo — script de seed do owner real, versão parametrizada para uso via `psql`/CLI, com os valores reais já preenchidos como referência do que foi de fato executado — a execução real em produção usou um bloco de SQL puro equivalente, colado diretamente no Dashboard pelo usuário, sem os metacomandos `\set`/`\echo` que o SQL Editor não suporta)
- `docs/stories/11.5.story.md` (modificado — checkboxes, status, Dev Agent Record, Change Log)

---

## QA Results

### Review Date: 2026-07-15

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Story atípica para revisão: não há código de aplicação, e — diferente de todas as stories anteriores do epic — o trabalho real (migration e seed) rodou em **produção**, executado pelo próprio usuário, não por mim nem pelo @dev. Isso muda a natureza desta revisão: não tenho credenciais de produção nesta sessão, então não posso reexecutar/reconferir os fatos de produção de forma independente como fiz nas Stories 11.0-11.2 (onde eu tinha acesso direto ao Postgres local via Docker). Esta revisão se apoia na evidência já coletada e no relato direto do usuário (colado nesta conversa), não em query própria — registro essa limitação explicitamente em vez de fingir uma verificação que não fiz.

### O que PUDE verificar de forma independente

- Consistência do script versionado (`epic11_owner_seed.sql`) com os valores realmente usados — confere.
- Aderência às 4 mitigações de risco (R1-R4) documentadas na própria story: R1 (IDs reais confirmados via consulta, não assumidos) ✓, R2 (dry run em 11.0 antes de produção, gate de autorização separado) ✓, R3 (idempotência via `ON CONFLICT DO NOTHING`, testada localmente) ✓, R4 (smoke test dos 2 admins reais pós-seed, sem alteração) ✓.
- O achado emergente (migration nunca aplicada em produção) foi tratado corretamente: escalado ao usuário antes de qualquer ação, não assumido unilateralmente — consistente com a regra do próprio epic ("em caso de dúvida sobre risco de regressão, a story para e escala").

### O que NÃO pude verificar de forma independente (limitação registrada, não escondida)

- Não confirmei eu mesma, via query direta em produção, que `user_profiles`/`user_locations` têm exatamente as linhas esperadas — baseio-me no resultado colado pelo usuário na conversa (que bate com o formato esperado do script).
- Não confirmei eu mesma o resultado do smoke test manual dos 2 admins reais — baseio-me no relato direto do usuário ("testei tudo, ficou igual, sem alteração para os admins").

### Compliance Check

- Coding Standards: [N/A]
- Project Structure: [✓]
- Testing Strategy: [✓] — dry run local antes de produção, conforme AC5
- All ACs Met: [✓] AC1-AC5, com a ressalva de que a confirmação de AC1/AC2 depende do relato do usuário, não de query própria (ver acima)

### Nit não-bloqueante para @dev

`File List` desta story tem a linha `docs/stories/11.5.story.md` duplicada (linhas 156-157 antes desta revisão). Cosmético, não afeta o gate — só limpar na próxima edição.

### Security Review

Nenhum problema. Credenciais de produção não transitaram por esta sessão em nenhum momento — o usuário optou corretamente pela opção mais segura (executar ele mesmo via Dashboard).

### Files Modified During Review

Nenhum (fora desta seção).

### Gate Status

Gate: PASS → docs/qa/gates/11.5-pass.yml

### Recommended Status

[✓ Ready for Done]
