# STORY 7.3: MetricCard — Cards de Métricas do Dashboard

**ID:** 7.3 | **Epic:** EPIC-07 — Dashboard Financeiro  
**Sprint:** 4 | **Points:** 3 | **Priority:** 🟠 High  
**Created:** 2026-05-19 | **Status:** ✅ Ready  
**Executor:** @dev | **Quality Gate:** @architect | **QG Tools:** coderabbit  
**Predecessor:** 7.2

---

## User Story

**Como** dono da loja,  
**Quero** ver os principais indicadores financeiros em cards visuais no dashboard,  
**Para que** eu tenha visão rápida do desempenho do negócio.

---

## Acceptance Criteria

- [ ] AC1: `MetricCard` em `src/components/dashboard/MetricCard.tsx` com props: `title`, `value`, `subtitle`, `icon`, `trend`
- [ ] AC2: 4 cards obrigatórios: Faturamento do Dia, Total de Vendas, Ticket Médio, Vendas por Turno
- [ ] AC3: Valores formatados em BRL com `Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })`
- [ ] AC4: Skeleton loading quando dados estão carregando (Shadcn `Skeleton`)
- [ ] AC5: Trend indicator: seta para cima (verde) ou baixo (vermelho) comparando com dia anterior
- [ ] AC6: Cards responsivos: 2 colunas em mobile, 4 em desktop

---

## Tasks

### T1 — MetricCard component (1.5h)
```typescript
interface MetricCardProps {
  title: string
  value: string | number
  subtitle?: string
  icon?: React.ReactNode
  trend?: { value: number; direction: 'up' | 'down' | 'neutral' }
  isLoading?: boolean
}
```

### T2 — Grid de 4 cards no DashboardPage (1h)
- [ ] `src/pages/DashboardPage.tsx`: grid responsivo
- [ ] Consumir `useDashboardMetrics` e mapear para props dos cards
- [ ] Faturamento do Dia: `total_amount` do `daily_summary` para hoje
- [ ] Total de Vendas: `total_sales` do dia
- [ ] Ticket Médio: `avg_ticket` calculado
- [ ] Vendas por Turno: breakdown por `shift_number`

### T3 — Trend calculation (0.5h)
- [ ] Comparar `today.total_amount` vs `yesterday.total_amount`
- [ ] `trend.direction = today > yesterday ? 'up' : 'down'`
- [ ] `trend.value = ((today - yesterday) / yesterday * 100).toFixed(1)` → "12.5%"

---

## Dev Notes

**Formatação BRL:**
```typescript
const fmt = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' })
fmt.format(1234.5) // "R$ 1.234,50"
```

**Skeleton:** Exibir quando `isLoading` — evitar layout shift. Usar `Skeleton` do Shadcn com mesmas dimensões do card.

**Trend comparativo:** Necessita de 2 dias de dados no `daily_summary`. Se ontem não tem dados (primeiro dia de operação), exibir trend como neutro.

---

## 🤖 CodeRabbit Integration

**Primary Type:** Frontend  
**Complexity:** Low

**Primary Agents:** @dev  
**Self-Healing:** @dev — light mode, max 2 iterações, CRITICAL only

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- Formatação BRL correta (vírgula decimal, ponto milhar)
- Skeleton com mesmas dimensões do card preenchido
- Trend não quebra com divisão por zero (yesterday = 0)

---

## Dependencies

**Depends on:** 7.2 (useDashboardMetrics)  
**Blocks:** 7.6 (layout do dashboard)

---

## Definition of Done

- [ ] 4 cards exibidos com dados reais
- [ ] Skeleton exibido durante loading
- [ ] Formatação BRL correta
- [ ] PR aprovado

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-19 | 1.0 | Story criada | River (SM) |
