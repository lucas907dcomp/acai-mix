# STORY 1.1: Setup do Projeto React + Vite + TypeScript + Tailwind + Shadcn

**ID:** 1.1 | **Epic:** EPIC-01 — Infraestrutura Base  
**Sprint:** 1 | **Points:** 5 | **Priority:** 🔴 Critical  
**Created:** 2026-05-19 | **Status:** ✅ Done  
**Executor:** @dev | **Quality Gate:** @architect | **QG Tools:** coderabbit

---

## User Story

**Como** desenvolvedor do AçaiMix,  
**Quero** um projeto React com Vite, TypeScript, Tailwind CSS e Shadcn/UI configurado e funcionando,  
**Para que** toda a equipe parta de uma base consistente com a stack definida na arquitetura.

---

## Acceptance Criteria

- [x] AC1: Projeto criado com `npm create vite@latest` usando template `react-ts`
- [x] AC2: Tailwind CSS v3 configurado e funcionando (classe `text-purple-600` visível no browser)
- [x] AC3: Shadcn/UI inicializado com `npx shadcn@latest init` — tema customizado com as cores do AçaiMix (Roxo Profundo `#4C1D95`, Verde-Menta `#6EE7B7`, Amarelo Quente `#FCD34D`)
- [x] AC4: Path aliases configurados no `tsconfig.app.json` e `vite.config.ts` (`@/` → `src/`)
- [x] AC5: `npm run dev` inicia sem erros e exibe página de boas-vindas em `localhost:5173`
- [x] AC6: `npm run build` completa sem erros TypeScript
- [x] AC7: `npm run lint` configurado (ESLint + Prettier) sem erros na base inicial
- [x] AC8: Estrutura de diretórios inicial criada conforme arquitetura

---

## Scope

**IN:**
- Scaffolding do projeto com a stack completa
- Configuração de Tailwind com cores customizadas do design
- Configuração de Shadcn/UI
- Estrutura inicial de pastas (`src/components`, `src/pages`, `src/stores`, `src/hooks`, `src/providers`, `src/lib`, `src/types`, `src/router`)
- ESLint + Prettier
- Path aliases

**OUT:**
- Qualquer lógica de negócio
- Componentes de UI específicos da aplicação
- Integração com Supabase
- Testes end-to-end

---

## Tasks

### T1 — Criar projeto Vite (0.5h)
- [x] `npm create vite@latest acai-mix -- --template react-ts`
- [x] Verificar `package.json` com React 19 + TypeScript (React 19 estável, conforme scaffold latest)

### T2 — Instalar e configurar Tailwind (0.5h)
- [x] `npm install -D tailwindcss postcss autoprefixer`
- [x] `npx tailwindcss init -p`
- [x] Configurar `tailwind.config.js` com `content` paths
- [x] Adicionar diretivas no `index.css`
- [x] Configurar cores customizadas: `purple-deep: #4C1D95`, `mint-green: #6EE7B7`, `warm-yellow: #FCD34D`

### T3 — Inicializar Shadcn/UI (0.5h)
- [x] `npx shadcn@latest init` — CLI com bug de permissão Windows (Ambiente de Impressão), componentes criados manualmente
- [x] Selecionar: style=new-york, baseColor=violet, cssVariables=yes
- [x] Instalar componentes base: `button`, `input`, `card`, `badge`, `dialog`
- [x] Verificar `components/ui/` gerado

### T4 — Configurar path aliases (0.5h)
- [x] Atualizar `tsconfig.app.json` com `"@/*": ["./src/*"]` (TS6+ paths sem baseUrl)
- [x] Atualizar `vite.config.ts` com `resolve.alias`
- [x] `@types/node` incluído em devDependencies

### T5 — Criar estrutura de diretórios (0.5h)
- [x] Criar `src/components/{ui,pdv,dashboard}/`
- [x] Criar `src/pages/`
- [x] Criar `src/stores/`
- [x] Criar `src/hooks/`
- [x] Criar `src/providers/scale/` e `src/providers/sync/`
- [x] Criar `src/lib/`, `src/types/`, `src/router/`

### T6 — Configurar ESLint + Prettier (0.5h)
- [x] Instalar dependências de lint (eslint, typescript-eslint, react-hooks, react-refresh)
- [x] `eslint.config.js` (flat config) e `.prettierrc` criados
- [x] Scripts `lint`, `lint:fix`, `format` adicionados ao `package.json`
- [x] Verificar `npm run lint` sem erros

### T7 — Verificação final (0.5h)
- [x] `npm run dev` → sem erros (localhost:5173)
- [x] `npm run build` → sem erros TypeScript
- [x] `npm run lint` → sem erros

---

## Dev Notes

**Stack de referência:** [Source: docs/architecture/acaipro-system-design.md#2.1]
- React 18 + Vite
- TypeScript
- Tailwind CSS
- Shadcn/UI
- Path alias `@/` → `src/`

**Cores do design (do Figma AçaiMix):**
- Roxo Profundo: `#4C1D95` (primary)
- Verde-Menta: `#6EE7B7` (accent)
- Amarelo Quente: `#FCD34D` (highlight)

**Estrutura de arquivos:** [Source: docs/architecture/acaipro-system-design.md#12]

---

## 🤖 CodeRabbit Integration

**Primary Type:** Deployment/Infrastructure  
**Complexity:** Low — setup e scaffolding sem lógica de negócio

**Primary Agents:** @dev, @devops  
**Self-Healing:** @dev — light mode, max 2 iterações, CRITICAL only

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- Configuração correta dos aliases de path
- Ausência de credenciais hardcoded
- Build sem erros TypeScript

---

## Dependencies

**Depends on:** Nenhuma (primeira story do projeto)  
**Blocks:** Todas as outras stories

---

## Definition of Done

- [x] Todos os ACs verificados manualmente no browser
- [x] `npm run dev`, `npm run build`, `npm run lint` todos passando
- [x] Estrutura de diretórios conforme arquitetura
- [ ] PR criado e aprovado

---

## Dev Agent Record

**Agent Model Used:** claude-sonnet-4-6

**Completion Notes:**
- Shadcn CLI (`shadcn@latest`) tem bug de permissão no Windows com diretório `Ambiente de Impressão` — componentes criados manualmente com código fonte oficial (button, input, card, badge, dialog)
- TypeScript 6 deprecou `baseUrl` — path alias `@/*` configurado sem `baseUrl` (TS6+ suporta `paths` standalone)
- ESLint flat config (`eslint.config.js`) com override para `src/components/ui/` (react-refresh desabilitado — padrão Shadcn exporta variants + components no mesmo arquivo)
- React 19 usado no lugar de React 18 (último estável no scaffold Vite 8)

**File List:**
- `package.json` — criado
- `index.html` — criado
- `vite.config.ts` — criado (com path alias `@/` → `src/`)
- `tsconfig.json` — criado
- `tsconfig.app.json` — criado (com paths alias)
- `tsconfig.node.json` — criado
- `tailwind.config.js` — criado (cores AçaiMix: purple-deep, mint-green, warm-yellow + CSS vars Shadcn)
- `postcss.config.js` — criado
- `eslint.config.js` — criado (flat config + override Shadcn)
- `.prettierrc` — criado
- `components.json` — criado (Shadcn config: new-york, violet, cssVariables)
- `.gitignore` — atualizado (entradas Vite adicionadas)
- `src/main.tsx` — criado
- `src/App.tsx` — criado (welcome page com cores AçaiMix)
- `src/index.css` — criado (Tailwind directives + CSS vars Shadcn violet)
- `src/vite-env.d.ts` — criado
- `src/lib/utils.ts` — criado (cn helper)
- `src/components/ui/button.tsx` — criado
- `src/components/ui/input.tsx` — criado
- `src/components/ui/card.tsx` — criado
- `src/components/ui/badge.tsx` — criado
- `src/components/ui/dialog.tsx` — criado
- `src/components/` — diretório
- `src/components/pdv/` — diretório
- `src/components/dashboard/` — diretório
- `src/pages/` — diretório
- `src/stores/` — diretório
- `src/hooks/` — diretório
- `src/providers/scale/` — diretório
- `src/providers/sync/` — diretório
- `src/lib/` — diretório
- `src/types/` — diretório
- `src/router/` — diretório

---

## QA Results

### Review Date: 2026-05-19

### Reviewed By: Quinn (Test Architect)

**7 Checks:** Code Review ✅ | Tests N/A ✅ | ACs ✅ (8/8) | Regressões N/A ✅ | Performance ✅ | Segurança ✅ | Documentação ✅

**Issues:** MNT-001 (medium), ARCH-001 (medium), MNT-002 (low), MNT-003 (low)

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.1-setup-react-vite-typescript-tailwind-shadcn.yml

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-19 | 1.0 | Story criada | River (SM) |
| 2026-05-19 | 1.1 | Validado GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-05-19 | 1.2 | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-05-19 | 1.3 | Development complete — Status: InProgress → InReview | @dev |
| 2026-05-19 | 1.4 | QA Gate CONCERNS — Status: InReview → Done | @qa |
