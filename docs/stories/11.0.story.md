# STORY 11.0: Ambiente de Teste — Stack Local Supabase + Seed Sintético

**ID:** 11.0 | **Epic:** EPIC-11 — Perfil "Dono" com Visão Multi-Loja
**Sprint:** 9 | **Points:** 3 | **Priority:** Crítica (bloqueante)
**Created:** 2026-07-14 | **Status:** InReview
**Executor:** @dev (com apoio de @data-engineer) | **Quality Gate:** @qa | **QG Tools:** manual (infra)
**Predecessor:** Nenhuma — primeira story do EPIC-11, bloqueante para todas as outras

---

## Referências

- Epic: `docs/stories/EPIC-11-perfil-dono-multi-loja.md`, seção 3.0 (F0)
- Doc de arquitetura: `docs/architecture/epic11-owner-multiloja-tech-decisions.md`, seção 5.6
- Migration já escrita (será aplicada ao subir o ambiente): `supabase/migrations/20260714000000_owner_multiloja.sql`
- Smoke test já escrito: `supabase/tests/epic11_rls_smoke.sql` (16 casos, T1-T16)

---

## User Story

**Como** desenvolvedor/@data-engineer do AçaiMix,
**Quero** um ambiente de teste local isolado (stack Supabase CLI + seed sintético), reproduzível a qualquer momento,
**Para que** eu possa aplicar e validar as migrations e o RLS do EPIC-11 sem tocar no único banco real existente hoje — o de produção, com as 2 lojas vendendo.

---

## Contexto (por que esta story existe)

Verificado durante a revisão da migration (@data-engineer): este projeto **não tem staging**. `.env`/`.env.example` apontam para um único projeto Supabase, e `package.json` usa `supabase gen types --linked` (projeto linkado = produção). O único banco disponível hoje é o de produção. O que já existe, mas nunca foi documentado como processo, é `supabase/config.toml` — a stack local do Supabase CLI, que sobe um Postgres/Auth isolado, aplica todas as migrations do zero e roda um seed. Esta story formaliza esse processo.

---

## Acceptance Criteria

- [ ] AC1: `supabase start` sobe um Postgres/Auth local isolado e aplica **todas** as migrations do repositório do zero (incluindo `20260714000000_owner_multiloja.sql`), reproduzindo o schema atual sem erros.
- [ ] AC2: Existe um seed sintético dedicado (`supabase/tests/epic11_seed_staging.sql`, distinto do `seed.sql` de produção) que cria, com UUIDs estáveis e documentados (os mesmos usados como placeholder em `epic11_rls_smoke.sql`):
  - 2 usuários `admin` (um por loja), simulando os 2 admins reais.
  - 1 usuário `owner` vinculado às 2 lojas de teste (via `user_locations`).
  - 1 usuário `staff` hipotético (para os testes T9 do smoke test).
  - 3 `locations` de teste: 2 vinculadas ao owner de teste, 1 fictícia e propositalmente NÃO vinculada (para os testes de vazamento T6, T7, T12).
- [ ] AC3: O processo (como subir, como resetar, como rodar o smoke test contra esse ambiente) está documentado em texto claro, acessível a @dev e @qa sem depender de conhecimento tácito.
- [ ] AC4: Nenhuma story subsequente deste epic (11.1 em diante) aplica migration em qualquer ambiente antes desta story estar concluída e validada.
- [ ] AC5: O ambiente é descartável e reproduzível a qualquer momento (`supabase db reset` ou equivalente) — não é um banco de "staging" persistente para gerenciar manualmente.

---

## Tasks

### T1 — Subir a stack local e validar migrations (1h)
- [x] Confirmar Supabase CLI instalado (`supabase --version`)
- [x] Rodar `supabase start` a partir da raiz do projeto
- [x] Rodar `supabase migration list` e confirmar que todas as migrations, incluindo `20260714000000_owner_multiloja.sql`, aplicaram sem erro
- [x] Se `supabase start` falhar em alguma migration antiga, documentar o erro exato antes de prosseguir — não é esperado, mas não deve ser silenciado

### T2 — Seed sintético (2h)
- [x] Criar `supabase/tests/epic11_seed_staging.sql`
- [x] Criar os usuários de `auth.users` necessários (nota: `auth.users` normalmente não aceita INSERT direto com senha utilizável — usar `supabase auth admin` da CLI ou a API local do GoTrue para criar os 4 usuários de teste; documentar o comando exato usado)
- [x] Inserir as 3 `locations` de teste
- [x] Inserir os `user_profiles` correspondentes (2 admin, 1 owner, 1 staff), cada um com o `location_id` inicial correto
- [x] Inserir as 2 linhas em `user_locations` vinculando o owner de teste às 2 lojas
- [x] Usar exatamente os mesmos UUIDs que já estão como placeholder em `supabase/tests/epic11_rls_smoke.sql` (`:admin_loja1`, `:admin_loja2`, `:owner_user`, `:loja1`, `:loja2`, `:loja3_fake`, `:staff_loja1`), para o smoke test funcionar sem edição

### T3 — Rodar o smoke test contra o ambiente (30min)
- [x] Rodar `psql <connection-string-local> -f supabase/tests/epic11_rls_smoke.sql`
- [x] Confirmar que todos os 16 casos executam sem erro de sintaxe/conexão (o **resultado** de cada teste — PASS/FAIL — é validado pelas stories 11.1/11.2, que tratam da lógica de RLS em si; esta story só garante que o ambiente permite rodar o script)

### T4 — Documentar o processo (30min)
- [x] Adicionar uma seção curta em `docs/architecture/epic11-owner-multiloja-tech-decisions.md` (ou um novo `docs/architecture/epic11-ambiente-teste.md`) com: comando para subir, comando para resetar, comando para rodar o smoke test, e onde ficam os UUIDs de teste
- [x] Deixar claro que este ambiente é efêmero — não guardar segredos reais nele, não usar para nada além de teste deste epic

---

## Dev Notes

**Esta story é sobre infraestrutura de teste, não sobre a lógica do EPIC-11.** A migration `20260714000000_owner_multiloja.sql` já existe e será aplicada automaticamente ao rodar `supabase start` (porque o Supabase CLI aplica todas as migrations do diretório em ordem). Isso significa que, tecnicamente, o schema do EPIC-11 já vai existir no ambiente local assim que esta story terminar — mas a **validação** de que esse schema se comporta como esperado (RLS correta, guarda-corpo funcionando) é trabalho das Stories 11.1 e 11.2, não desta.

**`auth.users` não aceita INSERT direto com senha usável.** O `seed.sql` de produção já documenta isso ("criar via Supabase Dashboard, depois inserir em user_profiles"). Para o ambiente local, usar a CLI (`supabase` tem comandos de auth admin) ou a API REST local do GoTrue (disponível em `http://localhost:54321` por padrão) para criar os usuários de teste com senha conhecida.

**Nunca rodar isto contra o projeto de produção.** A connection string usada em T1/T3 deve ser sempre a local (porta padrão 54322 para Postgres direto, ou via `supabase db` que já aponta para o ambiente certo quando não há `--linked`).

### Testing

- `supabase start` sobe sem erro e aplica todas as migrations.
- `supabase/tests/epic11_rls_smoke.sql` roda de ponta a ponta sem erro de conexão/sintaxe contra o ambiente local.
- Seed sintético cria exatamente os UUIDs esperados pelo smoke test (conferir via `SELECT` direto nas tabelas após rodar o seed).

---

## Dependencies

**Depends on:** Nenhuma.
**Blocks:** Stories 11.1, 11.2, 11.5, 11.3, 11.4 — nenhuma delas aplica migration em lugar nenhum antes desta estar concluída.

---

## Definition of Done

- [x] `supabase start` sobe do zero sem erro, aplicando toda a migration history incluindo a do EPIC-11
- [x] Seed sintético criado e documentado, com os UUIDs que o smoke test já espera
- [x] Smoke test roda de ponta a ponta contra o ambiente local (resultado de cada caso é responsabilidade das stories seguintes)
- [x] Processo de subir/resetar/testar documentado em local acessível à equipe
- [x] Nenhuma story seguinte foi iniciada antes desta ser concluída

---

## Dev Agent Record

### Agent Model Used

Dex (dev) — Sonnet 5, modo YOLO (autônomo)

### Debug Log References

- Bloqueio real #1: `supabase start` falhou inicialmente — porta 54322 ocupada por outro projeto Supabase local (`AlphaTracker`), não relacionado ao EPIC-11. Resolvido com aprovação explícita do usuário: `supabase stop --project-id AlphaTracker` (usuário confirmou que não usa mais essa instância local, só a versão em nuvem).
- Bloqueio real #2: Docker Desktop não estava respondendo (`docker ps` falhava) — iniciado via `Docker Desktop.exe`; ficou pronto quase imediatamente (o daemon já tinha containers de outros projetos rodando).
- `supabase migration list` ficou pendurado esperando input interativo (provável prompt de senha do banco remoto **vinculado à produção**, já que `package.json`/CLI usam `--linked`). Interrompido deliberadamente para não arriscar tocar produção. Evidência de sucesso do AC1 usada em seu lugar: log de `supabase start`, que mostra as 16 migrations aplicando sem erro (incluindo `20260714000000_owner_multiloja.sql`), com apenas `NOTICE` benignos de objetos já existentes.
- `auth.users`/`auth.identities`: CLI local (v2.99.0) não expõe subcomando de auth admin, e a API do GoTrue não permite fixar o UUID do usuário criado. Optado por INSERT direto via `pgcrypto` (`crypt()` + `gen_salt('bf')`), padrão documentado pela comunidade Supabase para este cenário — decisão registrada no próprio `epic11_seed_staging.sql` e em `docs/architecture/epic11-ambiente-teste.md`.
- `psql` não está no PATH do host (Windows) — usado `docker exec -i supabase_db_acai-mix psql ...` como alternativa, documentado em `epic11-ambiente-teste.md` seção 3.

### Completion Notes List

- Ambiente local sobe do zero sem erro, aplicando as 16 migrations do repositório incluindo a do EPIC-11.
- Seed sintético criado e validado: 4 usuários reais em `auth.users`/`auth.identities` (autenticáveis via GoTrue local), 3 `locations` de teste, 4 `user_profiles`, 2 vínculos em `user_locations`. Auto-checagem do próprio script confirma as contagens esperadas.
- Smoke test (`epic11_rls_smoke.sql`) rodou ponta a ponta, 16/16 casos sem erro de sintaxe/conexão. Observação não-bloqueante para as Stories 11.1/11.2: nos casos T15 e T16, o bloqueio por RLS se manifesta como `UPDATE 0` silencioso (comportamento padrão do Postgres para exclusão via cláusula `USING`), não como exceção — documentado em `epic11-ambiente-teste.md` seção 4 para evitar confusão na leitura dos resultados.
- Processo documentado em `docs/architecture/epic11-ambiente-teste.md` (novo arquivo dedicado, opção prevista em T4), referenciado a partir da seção 5.6 do doc de decisões técnicas.
- Nenhuma migration foi aplicada em produção ou em ambiente que não o local (`127.0.0.1:54322`). O único momento de risco (`migration list` pendurado aguardando credencial do projeto vinculado) foi interrompido antes de qualquer interação com produção.
- CodeRabbit self-healing: **não executado** — `wsl bash` retornou "bash: not found" (a única distro WSL registrada é `docker-desktop`, interna do Docker Desktop, sem distribuição Linux de usuário instalada). Comportamento é o fallback documentado em `dev-develop-story.md` ("command not found" → skip self-healing, revisão manual necessária). Revisão manual do diff foi feita por este agente antes da transição de status; nenhum segredo real, credencial ou dado de produção presente nos arquivos novos/modificados — apenas UUIDs sintéticos e senha de teste (`Test@1234`) documentada como tal.

### File List

- `supabase/tests/epic11_seed_staging.sql` (novo)
- `docs/architecture/epic11-ambiente-teste.md` (novo)
- `docs/architecture/epic11-owner-multiloja-tech-decisions.md` (modificado — referência à seção 5.6)
- `docs/stories/11.0.story.md` (modificado — checkboxes, status, Dev Agent Record, Change Log)

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-14 | 1.0 | Story criada a partir do sharding do EPIC-11 (feature F0) | River (SM) |
| 2026-07-14 | 1.1 | Validação: GO (9/10) — dependência com stories 11.1+ confirmada como formal (campo Predecessor + seção Dependencies em cada uma, não apenas texto). Observação não-bloqueante: AC4 duplica a seção Dependencies (aceitável). Status: Draft → Ready | @po (Pax) |
| 2026-07-15 | 1.2 | Development started (yolo mode) — Status: Ready → InProgress | @dev |
| 2026-07-15 | 1.3 | Development complete — Status: InProgress → InReview | @dev |
